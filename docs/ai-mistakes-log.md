# AI mistakes log

- Initial PowerShell `curl` alias broke recon fetches; switched to `curl.exe`.
- Store bundle obfuscates price API strings; avoided guessing endpoints and used DOM scraping instead.
